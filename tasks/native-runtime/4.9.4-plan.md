# Implementation Plan: 4.9.4 Python De-authoritization

<!-- docs-language-switcher:start -->
[中文](../../README.md) / [English](../../README.en.md)
<!-- docs-language-switcher:end -->

## Overview

Version 4.9.4 de-authoritizes Python in the default production topology while retaining an explicit,
bounded rollback package (`DEEPSEEK_LEGACY_PYTHON=1`). The default production deployment runs exclusively:
- `deepseek-edge` (Rust)
- `deepseekd` (Go)
- `deepseek-worker` (Rust)
- `frontend` (static assets)

## Task List

### Phase 1: Native Production Topology Packaging
- [x] Create `docker-compose.native.yml` defining pure Native Rust + Go services
- [x] Create `go/Dockerfile` with `CGO_ENABLED=0` and Go 1.27.1
- [x] Bind edge listener to port 8000 and proxy `/api/*` to `deepseekd` (port 8090)

### Phase 2: Python De-authoritization & Rollback Safeguards
- [x] Enforce `assert_production_python_allowed()` preventing unflagged Python server starts
- [x] Require `DEEPSEEK_LEGACY_PYTHON=1` for emergency rollback
- [x] Write `tests/test_native_runtime_topology.py`

### Phase 3: Verification & Parity
- [x] Verify zero Python production services in native topology
- [x] Run full native Python pytest suite
- [x] Verify `ruff check .` and `mypy .`
