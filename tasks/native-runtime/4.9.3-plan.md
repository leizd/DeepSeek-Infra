# Implementation Plan: 4.9.3 Go Full Control Authority

<!-- docs-language-switcher:start -->
[中文](../../README.md) / [English](../../README.en.md)
<!-- docs-language-switcher:end -->

## Overview

Version 4.9.3 moves every control domain to Go through fenced, reversible ownership cutovers.
Old Python writers are mechanically denied, and Go controller kill/takeover preserves exactly-once
effect identity and fencing.

Status: **in progress**. The checkpoint compatibility, mechanical-denial primitives,
isolated SQLite shadow store, and authenticated Go-to-Rust epoch-install channel
below are implemented, but
`release/native_runtime_go_control_store_v1.json` still declares `mode: shadow`. No production
control domain is claimed as cut over. SQLite persistence and signed epoch install are
substrates for later authority work; they are not production control-plane ownership.

## Task List

### Phase 1: Go Control Authority Checkpoints
- [x] Implement `AuthorityCheckpoint` in `go/internal/store/authority.go`
- [x] Add validation and secret-stripping tests in `go/internal/store/authority_test.go`
- [x] Support the frozen Python `control-authority-v1` two-stage digest and contiguous chain rules in Go
- [x] Replay the shared `compat/native-runtime/v1/control/authority_checkpoints.json` corpus through both Python and Go
- [x] Enforce genesis, idempotent replay, next-generation, fork, stale-writer, gap, and previous-digest head CAS rules in Go

### Phase 2: Python Mechanical Writer Denial
- [x] Implement `deepseek_infra/infra/native_runtime/authority.py`
- [x] Add `assert_python_writer_allowed` mechanically raising `PythonWriterMechanicallyDeniedError` when Go is authoritative
- [x] Add `assert_production_python_allowed` preventing un-opted legacy Python startup
- [x] Write `tests/test_native_runtime_mechanical_denial.py`

### Phase 3: Domain Ownership Transition
- [x] Validate all 18 Go-owned control domains in `release/native_runtime_ownership_v1.json`
- [x] Ensure store path isolation (`go-control/`) with zero shared SQLite files
- [x] Replace the shadow file-store scaffold with the transactional Go control database and reversible migrations
- [x] Freeze `control-authority-request-v1` canonical bytes, digest, and fail-closed verification (compat v7)
- [x] Wire the frozen request into an authenticated Go-to-Rust transport before any worker can install a live epoch
- [x] Persist a per-domain cutover state machine (`shadow` → `dual_evaluate` → `go_authoritative` → `python_shadow` → `python_disabled`) with fencing, revision, epoch, and an append-only journal. `go_authoritative` remains `CUTOVER_NOT_AUTHORIZED`; `MutateProduction` remains mechanically denied. First candidate domain is `policy`.
- [x] Freeze `control-mutation-request-v1` (compat v17) with canonical bytes, digest, replay/operation-id conflict, and fail-closed Python/Go/Rust verification. Payload is a `policy` `shadow-compare` intent. `MutateProduction` remains mechanically denied.
- [x] Persist a replay-safe operation journal (`control_operations`) that authenticates a signed `control-mutation-request-v1`, fences the writer, and records `PROPOSED` without applying production state. Same `operationId` retries return `ALREADY_APPLIED`; payload/request/nonce conflicts fail closed. `MutateProduction` remains mechanically denied. `go_authoritative` remains `CUTOVER_NOT_AUTHORIZED`.
- [x] Persist Rust worker epoch installs and request/nonce reservations atomically in isolated `rust-worker/authority.sqlite3`. Authority-configured binaries require a durable state root; process restart cannot select an in-memory fallback. This does not authorize provider effects or production control mutation. See [worker authority journal](../../docs/NATIVE_WORKER_AUTHORITY_STORE.md).
- [ ] Authorize production mutation through that authenticated channel. `MutateProduction` remains mechanically denied
- [ ] Cut over each domain through shadow, dual-evaluate, Go-authoritative, Python-shadow, then Python-disabled
- [ ] Hand off ownership / fencing authority per control domain
- [ ] Prove shadow → authority switching with a production rollback path

### Phase 4: CI & Release Gates
- [ ] Run the full native and Python compatibility suites on the exact release head
- [ ] Run `ruff check .`, `mypy .`, Go race/coverage, and Rust coverage on the exact release head
- [ ] Verify zero regression across offline eval, security, process-kill, and provider-backed runners
- [ ] Collect Ubuntu CI `go test -race ./...` evidence; local Windows/MinGW race is not authoritative

## Completed in the SQLite shadow-store slice

- JSON shadow file store (`manifest.json` / `writer.json` / atomic replace) removed from the production Go path. Legacy markers are rejected and left in place.
- Isolated database path `go-control/control.sqlite3` with Python path and Python SQLite filename rejection.
- Versioned schema migration in one transaction: bootstrap metadata, strict domain tables, append-only `control_events`, immutability triggers, `PRAGMA user_version`.
- Durable writer lease / fencing in `control_writer`, claimed and renewed inside `BEGIN IMMEDIATE` transactions.
- Record CAS on revision plus legal state transitions; fenced domains validate execution epochs in the same transaction.
- Event journal insert is in the same transaction as the record write; later-statement failure rolls back both.
- Corruption detection: digest mismatch, non-canonical payload, unknown state, orphaned/missing events, regressing event metadata, schema/revision/state inconsistency.
- Failure injection proves record/event/writer/migration/rollback abort does not commit partial domain state.
- Symlink database files are rejected. Identity inspection uses an immutable read-only SQLite URI so a foreign file is not modified by WAL/SHM or a writer lease before it is rejected.
- Unknown SQLite user objects, incomplete Go control markers, extra migrations, and `user_version`/`schema_version` mismatch fail closed.
- SIGKILL-style recovery is allowed only for an empty SQLite header with zero user objects. Arbitrary foreign SQLite files are not auto-adopted.
- Control payloads recursively reject passwords, tokens, credentials, and PEM/Age private-key material, including nested objects, arrays, and punctuation/case variants. `credentialReference`, digests, IDs, providers, and `fencingToken` remain allowed.
- SQLite driver is `modernc.org/sqlite` (CGO-free). `MutateProduction` still returns `MUTATION_DENIED`.
- Catalog `release/native_runtime_go_control_store_v1.json` remains `mode: shadow`.
- Frozen `control-authority-request-v1` (compat v7) with canonical bytes, digest, and fail-closed Python/Go/Rust verification.
- Frozen `control-mutation-request-v1` (compat v17) with canonical bytes, digest, replay/operation-id conflict, and fail-closed Python/Go/Rust verification. The payload is a `policy` `shadow-compare` intent and does not authorize `MutateProduction`.
- Schema v2 persists per-domain cutover state and an append-only cutover journal. Every domain starts in `shadow` with Python as owner. `policy` is the first dual-evaluate candidate. Transitions to `go_authoritative` (and later) return `CUTOVER_NOT_AUTHORIZED`. Store catalog `mode` remains `shadow`.
- Schema v3 persists an append-only `control_operations` journal. `AcceptMutation` verifies a signed `control-mutation-request-v1` against the live cutover fence/epoch/revision, renews the writer lease, and inserts `PROPOSED`. It does not `Put` domain records and does not call `MutateProduction`. `go_authoritative` still returns `CUTOVER_NOT_AUTHORIZED`. Store catalog `mode` remains `shadow`.
- Additive Worker RPC `InstallAuthoritativeEpoch` carries the signed canonical document plus a matching `ActionFence`. Rust verifies the document against local signer/fleet/environment/fencing/replay state before installing a live epoch. Go signs with a dedicated `ctrl-signer-*` key and never treats a local verify as production authority. `MutateProduction` remains `MUTATION_DENIED`. Shadow mode is unchanged.

## Explicitly not done

- Production mutation authorization. The authenticated channel can install a worker live epoch in shadow mode and journal a `PROPOSED` mutation request; it does not apply that journal to domain rows, authorize `MutateProduction`, or switch the Go control store out of shadow. Go must not locally pretend a Rust rejection succeeded. The in-process `install_authoritative_epoch` helper remains test-only and is not exposed as an unauthenticated RPC.
- Control-domain-by-control-domain cutover, ownership handoff, or fencing-authority migration.
- Production rollback path and shadow → authority switch proof.
- Go holding Federation / production root private keys, or storing secret material in control SQLite.
- Ubuntu CI race evidence. Local Windows `go test -race` is blocked by an existing MinGW/toolchain defect and is **not** recorded as passing.
- Exact-head release PASS for ruff/mypy/eval/security/provider runners.

## Current Development Evidence

The transactional control-store slice is locally verified on 2026-09-04:

- `gofmt -l .`, `go vet ./...`, and `go test ./...` pass with Go 1.27.1.
- The store and shadow suites pass with `CGO_ENABLED=0`; the SQLite driver adds no cgo/FFI boundary.
- `go mod tidy` and `go mod verify` succeed.
- `python scripts/check_go_coverage.py --dir . --min 95.0` passes at 95.6%. The 95.0% floor is unchanged.
- All 65 `test_native_runtime_*.py` tests, `native_runtime_contract.py --check`, and ruff/mypy on the touched Python contract test pass.

The v7 authority-request freeze is locally verified on 2026-09-04:

- Python oracle, Go `VerifyAuthorityRequestDocument`, and Rust `verify_authority_request_document` replay the same frozen success and failure vectors.
- `cargo test` / Clippy `-D warnings` for `deepseek-worker` pass on `1.85.0-x86_64-pc-windows-gnu`.
- `python scripts/check_go_coverage.py --dir . --min 95.0` passes at 95.2% after adding the verifier. The 95.0% floor is unchanged.

The per-domain cutover state-machine slice is locally verified on 2026-09-05:

- Schema v2 seeds every control domain in `shadow` with Python as owner. `policy` can enter `dual_evaluate` and roll back to `shadow` with a new epoch and fencing token. `go_authoritative` remains `CUTOVER_NOT_AUTHORIZED`.
- `gofmt`, `go vet ./...`, `go test ./...`, `go mod verify`, and `CGO_ENABLED=0` store/worker tests pass with Go 1.27.1.
- `python scripts/check_go_coverage.py --dir go --min 95.0` passes at 95.0%. The 95.0% floor is unchanged.
- `python scripts/native_runtime_contract.py --check` and the Go control-store catalog tests pass. Ownership contract `current_production_authority` remains `python`.

The replay-safe operation journal is locally verified on 2026-09-05:

- Schema v3 creates append-only `control_operations` with immutability triggers. `AcceptMutation` journals `PROPOSED` after authenticating the signed request against the live cutover. Retries of the same `operationId` and payload return `ALREADY_APPLIED`. `policy` dual-evaluate still journals only. `go_authoritative` remains `CUTOVER_NOT_AUTHORIZED`. `MutateProduction` remains `MUTATION_DENIED`.
- `gofmt`, `go vet ./...`, `go test ./...`, `go mod verify`, and `CGO_ENABLED=0` store/worker tests pass with Go 1.27.1.
- `python scripts/check_go_coverage.py --dir go --min 95.0` passes at 95.2%. The 95.0% floor is unchanged.
- `python scripts/native_runtime_contract.py --check` and the Go control-store catalog tests pass. Ownership contract `current_production_authority` remains `python`. Catalog `mode` remains `shadow`.

The authenticated epoch-install transport is locally verified on 2026-09-04:

- Additive `Worker.InstallAuthoritativeEpoch` is accepted by the frozen v1 descriptor baseline (additive-only). ControlPlane still exposes only `Health` and `ShadowEvaluate`.
- Rust installs a live epoch only after verifying the signed `control-authority-request-v1` document against local signer, fleet, environment, fencing token, and replay state. Envelope fence must match the signed action identity.
- Unconfigured workers reject install with `AUTHORITY_REQUEST_SIGNER_MISMATCH` and keep admission at `FENCE_MISMATCH`.
- Retry of an already-installed epoch fails closed with `STALE_EXECUTION_EPOCH`. Execute after a successful install still returns `STORAGE_NOT_AUTHORITATIVE`. Go `MutateProduction` remains `MUTATION_DENIED`.
- `gofmt`, `go vet ./...`, `go test ./...`, `go mod verify`, and `CGO_ENABLED=0` store/worker tests pass with Go 1.27.1. Coverage is 95.2% (`--min 95.0`).
- `cargo +1.85.0-x86_64-pc-windows-gnu test/clippy -D warnings` for `deepseek-protocol` and `deepseek-worker` pass.
- `python scripts/native_runtime_contract.py --check` and the `tests/test_native_runtime_*.py` suite pass.

This is development evidence, not an exact-head release PASS. Windows `go test -race`
remains unavailable because the local MinGW runtime cannot start the race binary;
the checksum-pinned Ubuntu CI job remains authoritative for the race gate. This
does not prove the SQLite store is race-free.

The Rust worker durable-authority slice is locally verified on 2026-09-06:

- Signed epoch installation, request-ID/nonce reservation, and acceptance time commit as one append-only SQLite row. Every admission reads the current database fencing token and epoch; independent handles cannot rely on stale process-local state.
- A real Rust binary was force-terminated after its gRPC acknowledgment and restarted. The installed epoch remained usable, replay was rejected, and effect lookup remained `EFFECT_UNKNOWN`. A separate killed SQLite subprocess left a hot rollback journal; reopening recovered the prior committed epoch/token and discarded uncommitted state.
- Independent review found and fixed read-only hot-journal recovery failure, a SQL wildcard that hid foreign tables, and a concurrent first-open initialization window. Atomic exclusive file creation, a worker-specific application ID, literal schema-name matching, and recovered-schema/signature verification now cover these boundaries.
- All 326 Rust workspace tests pass with zero failures or ignored tests. Workspace Clippy with warnings denied and Rust formatting pass on Rust 1.85 GNU. Go 1.27.1 worker/store tests and the real Go-to-Rust authenticated installation integration test pass.
- The 27 focused Python foundation/corpus/authority tests, Ruff, mypy on the three touched Python files, and the complete registered corpus gate (40 corpora, 29 versions) pass. CI supplies a dedicated worker state root.
- This does not prove a real provider effect, native process-kill storage takeover, production mutation authorization, the performance SLO, Ubuntu race/coverage gates, or exact-head release readiness. The worker remains unable to execute storage/transfer/signing effects, and Go production mutation stays denied.
