# Implementation Plan: 4.9.2 Rust Edge Authority

<!-- docs-language-switcher:start -->
[中文](../../README.md) / [English](../../README.en.md)
<!-- docs-language-switcher:end -->

## Overview

Version 4.9.2 makes Rust the only public listener and owner of all fast/data/security endpoints.
Public routes (`/healthz`, `/metrics`, `/v1/models`, `/v1/chat/completions`, `/mcp`, `/.well-known/agent-card.json`, `/a2a`)
are served natively by `deepseek-gateway`, with `/api/*` reverse-proxied to the Go control plane.

## Task List

### Phase 1: Rust Gateway Edge Routes
- [x] Wire all public routes from `compat/native-runtime/v1/http/rest_inventory.json` into `deepseek-gateway/src/lib.rs`
- [x] Add native handlers for `/.well-known/agent-card.json` and `/a2a`
- [x] Implement reverse-proxy routing for `/api/*` to Go control plane

### Phase 2: Route Inventory Parity Gate
- [x] Implement `tests/test_native_runtime_http_inventory.py` verifying full public route inventory coverage
- [x] Verify eventual Rust and Go ownership mappings

### Phase 3: Rust Crates Quality
- [x] Verify `cargo check -p deepseek-gateway` passes cleanly
- [x] Verify no dead routes or missing handlers

### Phase 4: CI & Release Gates
- [x] Run full native Python pytest suite
- [x] Run `ruff check .` and `mypy .`
- [x] Verify zero regression across offline eval and security runners
