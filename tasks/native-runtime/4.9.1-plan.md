# Implementation Plan: 4.9.1 Rust Storage & Transfer Plane

<!-- docs-language-switcher:start -->
[中文](../../README.md) / [English](../../README.en.md)
<!-- docs-language-switcher:end -->

## Overview

Version 4.9.1 executes the Rust Storage and Transfer Plane cutover across 13 data and security domains.
Python is completely removed from all production payload-byte paths while preserving exact storage,
encryption, federation, and proof semantics.

## Task List

### Phase 1: Rust Wire Bindings & Manifest Schemas
- [x] Implement `ObjectSet` (`object-set-v1`) in `rust/crates/deepseek-storage/src/object_set.rs`
- [x] Implement `ReceiptV4` and `CommitV4` in `rust/crates/deepseek-storage/src/receipt.rs`
- [x] Implement `ReplicaAttestation` and `FailureDomainMetadata` in `rust/crates/deepseek-federation/src/attestation.rs`
- [x] Implement `EvidenceProofEnvelope` in `rust/crates/deepseek-proof/src/envelope.rs`
- [x] Implement `TransferCheckpoint` in `rust/crates/deepseek-transfer/src/job.rs`

### Phase 2: Native Wire Parity Verification
- [x] Add `tests/test_native_runtime_storage_bindings.py` validating complete field-set coverage
- [x] Validate against frozen wire inventories (`storage/wire_inventory.json`, `federation/wire_inventory.json`, `evidence/envelope.json`)

### Phase 3: Rust Crates Quality & Clippy
- [x] Add serde and sha2 workspace dependencies across storage/transfer/federation/proof crates
- [x] Verify `cargo clippy` and `cargo check` compile cleanly

### Phase 4: CI & Release Gates
- [x] Verify `scripts/check_native_contract_parity.py` passes
- [x] Verify all native runtime Python tests pass
- [x] Run `ruff check .` and `mypy .`
